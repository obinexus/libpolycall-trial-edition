// src/utils/functional/index.js
const pipe = require('./pipe');
const curry = require('./curry');
const map = require('./map');
const reduce = require('./reduce');
const filter = require('./filter');

module.exports = {
    pipe,
    curry,
    map,
    reduce,
    filter
};